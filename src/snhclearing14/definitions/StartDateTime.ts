
/**
 * startDateTime
 * @targetNSAlias `tns`
 * @targetNamespace `http://www.emobility-partner.de`
 */
export interface StartDateTime {
    /** s:string|length,whiteSpace,pattern */
    LocalDateTime: string;
}
